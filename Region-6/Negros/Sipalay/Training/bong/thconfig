encoding  utf-8
source bong.th
# Change name to the main therion file


#Surveys to output
#-----------------

export map -proj plan -layout local -o bong.pdf
#Produces plan of cave, on a single sheet

export map -fmt kml -proj plan -layout local -o bong.kml

#export map -proj extended -layout local -o bongEe.pdf

export model -fmt survex -o bongth.3d
#Survex file showing centre line only

#export model -o bong.lox
#3d therion model to be viewed in the therion viewer

#export map -proj plan -o bong_Walls.3d
#Survex file showing walls only

layout local
  debug off
  map-header 0 0 ne
  #symbol-hide group cave-centreline
endlayout
