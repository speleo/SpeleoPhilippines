encoding  utf-8
source jurness20lastchamber.th
# Change name to the main therion file


#Surveys to output
#-----------------

export map -proj plan -layout local -o .\_output\jurness20lastchamber.pdf
#Produces plan of cave, on a single sheet

#export map -proj extended -layout local -o .\_output\jurness20lastchamberEe.pdf

export model -fmt survex -o .\_output\jurness20lastchamberth.3d
#Survex file showing centre line only

#export model -o .\_output\jurness20lastchamber.lox
#3d therion model to be viewed in the therion viewer

#export map -proj plan -o .\_output\jurness20lastchamber_Walls.3d
#Survex file showing walls only

layout local
debug off
map-header 0 0 ne
#symbol-hide group cave-centreline
endlayout
