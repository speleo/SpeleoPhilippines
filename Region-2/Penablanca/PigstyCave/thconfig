encoding  utf-8
source pigsty-cave.th
# Change name to the main therion file

#Surveys to output
#-----------------

export map -proj plan \
  -layout local \ 
  -o pigsty-cave.pdf
#Produces plan of cave, on a single sheet

#export map -proj extended -layout local -o pigsty-caveEe.pdf

export model -fmt survex -o pigsty-caveth.3d
#Survex file showing centre line only

#export model -o pigsty-cave.lox
#3d therion model to be viewed in the therion viewer

#export map -proj plan -o pigsty-cave_Walls.3d
#Survex file showing walls only

#inject standard code for layouts
  input ..\..\..\CodeLibrary\_translations.txt
  input ..\..\..\CodeLibrary\_layouts.mp

layout local
#copy scalebar_large_format
#copy legend_large_format
#copy legend_large_format_20U
#copy grid_2_continuous
copy bats
legend on
  debug off
  map-header 0 0 ne
  #symbol-hide group cave-centreline

endlayout
