encoding  utf-8
source upper_squeez.th
# Change name to the main therion file


#Surveys to output
#-----------------

#export map -proj plan -layout local -o upper_squeez.pdf
#Produces plan of cave, on a single sheet

#export map -proj extended -layout local -o upper_squeezEe.pdf

export model -fmt survex -o upper_squeezth.3d
#Survex file showing centre line only

#export model -o upper_squeez.lox
#3d therion model to be viewed in the therion viewer

#export map -proj plan -o upper_squeez_Walls.3d
#Survex file showing walls only

layout local
debug off
map-header 0 0 ne
#symbol-hide group cave-centreline
endlayout
