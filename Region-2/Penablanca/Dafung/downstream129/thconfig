encoding  utf-8
source downstream129.th
# Change name to the main therion file


#Surveys to output
#-----------------

#export map -proj plan -layout local -o downstream129.pdf
#Produces plan of cave, on a single sheet

#export map -proj extended -layout local -o downstream129Ee.pdf

export model -fmt survex -o downstream129th.3d
#Survex file showing centre line only

#export model -o downstream129.lox
#3d therion model to be viewed in the therion viewer

#export map -proj plan -o downstream129_Walls.3d
#Survex file showing walls only

layout local
debug off
map-header 0 0 ne
#symbol-hide group cave-centreline
endlayout
